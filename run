#!/usr/bin/env bash
set -euo pipefail
project_root="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$project_root"
export MPLCONFIGDIR="${MPLCONFIGDIR:-$project_root/.cache/matplotlib}"
exec "$project_root/.venv/bin/python" -m safety_conditioning "$@"
