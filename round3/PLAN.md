# Round 3: additional tasks and BayesFP

Preserve the completed Pendulum/Push-T results and their locked code. Work in this separate dependency overlay. No outcome-dependent replacement of conditional samples is allowed.

The requested comparisons are: (1) exact conditioning by first-feasible rejection, (2) exact Euclidean projection of a base sample over the same full horizon, (3) finite projected-Langevin refinement, and (4) BayesFP's published DDPM/Feynman–Kac particle procedure. BayesFP's raw output and BayesFP followed by the identical terminal projection are separate arms. Native task reward is not included in BayesFP's guidance cost in the primary comparison; its cost penalizes only violation of the supplied constraint. This keeps the comparison about safety filtering and distribution shape.

No official BayesFP implementation was found in the paper's links or a GitHub repository search (the one search result was unrelated Bayesian FPCA software). Implement the published Algorithm 1 / Appendix G.4 and label it **paper-derived reimplementation**, retaining the primary PDF/HTML and hashes. Validate zero-guidance equivalence to the original DDPM, analytic cost gradients, resampling behavior, and particle diagnostics before using it. Use 32 particles, all 100 DDPM steps, active resampling interval [0.05,0.95], period one and constant inverse temperature. Development strengths: 1, 5, 20, 100, 500. Choose on development feasibility and conditional-distribution discrepancy, not held-out rewards.

For Pendulum, use existing development cases for tuning, then new confirmation seed range 96000 onward. Preserve the existing DDPM and Langevin schedule. Include an equal-score-evaluation BayesFP setting with 12 particles (1,200 evaluations versus 1,124 for base+refinement), alongside published 32-particle configuration (3,200 evaluations). Report measured wall time and actual safety, not only quality of successful outputs.

Explore **Walker2d-v5** and **HalfCheetah-v5**, acquiring Farama-Minari expert and medium SAC weights by pinned revision. First run untouched 1,000-control environment/policy smoke episodes and compare both experts from shared simulator states. This is initially an RL feasibility gate, not a diffusion result. Only train a modest state diffusion imitation model after verifying useful controllers. Collect complete paired episodes, split by initial-state group before chunking, normalize on training data only, and validate the first generated chunk plus a fixed continuation before safety evaluation.

Use 0.4 s candidate chunk horizons: Walker2d 50 controls at 0.008 s, HalfCheetah 8 controls at 0.05 s. A full-chunk RMS torque envelope is the first constraint: RMS(A) <= c, together with existing action bounds. The global projection is radial scaling (the base samples are already within [-1,1]); this preserves action bounds and exactly minimizes Euclidean chunk displacement. This constraint couples the entire chunk and models an aggregate actuator-effort budget. For refinement, projection onto the intersection with action bounds must be exact, not merely radial clipping followed by coordinate clipping. Shared-state mode diversity, support and policy quality are measured, not inferred from task dimension.

Native rewards and failure termination remain untouched. Tune constraints on development cases, then write the final confirmation configuration before generating new held-out proposal banks. Retain weak, negative, infeasible-support and low-quality-policy outcomes. Broader claims require broader successful evidence; the current two mirrored Pendulum cases remain one task.
